apply plugin: 'com.android.application'

android {
    signingConfigs {
        acil {
            keyAlias 'acil'
            keyPassword 'acil4321'
            storeFile file('D:/Android/ws_studio/p/Android-Custom-Intent-List/Shayan/acil.jks')
            storePassword 'acil4321'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.3"
    defaultConfig {
        applicationId "com.android_custom_intent_list"
        minSdkVersion 8
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.acil
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:appcompat-v7:23.2.1'
}
